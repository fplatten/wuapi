package com.first.wuapi;

import java.io.File;
import java.io.IOException;
import java.net.URL;

import org.codehaus.jackson.JsonFactory;
import org.codehaus.jackson.JsonNode;
import org.codehaus.jackson.JsonParser;
import org.codehaus.jackson.map.ObjectMapper;
import org.jsonschema2pojo.SchemaMapper;
import org.junit.Test;

import com.first.wuapi.domain.conditions.CurrentObservation;
import com.sun.codemodel.JCodeModel;

public class JsonPojoMappingTest {
	
	@Test
	public void testMapping(){
		
		

		
			
//			File f = new File("C:/Users/fplatten/workspace/wuapi/src/test/resources/input.json");
//			
//			ObjectMapper mapper = new ObjectMapper();
//			mapper.configure(org.codehaus.jackson.map.SerializationConfig.Feature.INDENT_OUTPUT, true);
//			JsonFactory factory = mapper.getJsonFactory();
//			JsonParser jp = factory.createJsonParser(br);
//			JsonNode rootNode = mapper.readTree(jp);
//			
//			CurrentObservation co = mapper.readValue(new File("user.json"), CurrentObservation.class);
//			
			
		
		
	}

}
